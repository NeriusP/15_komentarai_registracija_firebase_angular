import { ProgBarDirective } from './prog-bar.directive';

describe('ProgBarDirective', () => {
  it('should create an instance', () => {
    const directive = new ProgBarDirective();
    expect(directive).toBeTruthy();
  });
});
